// import React from "react";
import InvoicesCard from "../incoices/InvoicesCard";
// import { useCustomQuery } from "@/hooks/useQuery";

const Invoices = () => {
  // const { data } = useCustomQuery("/invoices", ["invoices"]);
  return (
    <>
      <InvoicesCard />
    </>
  );
};

export default Invoices;
